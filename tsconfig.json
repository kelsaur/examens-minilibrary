{
  "compilerOptions": {
      "target": "ES2015", // Vilken JS version ska vi kompilera till
      "outDir": "./dist", // Var ska JS-filen hamna? I vilken mapp?
      "lib": ["ES2015", "DOM"]
  },
  "include": ["./src/script.ts"], // Vad för filer ska vi kompilera till JS?
  "exclude": [] // Vad ska vi inte kompilera?
}